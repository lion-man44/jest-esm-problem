//require = require('@std/esm')(module, { esm: 'js', cjs: true });
import { sum } from './sum.js'

import { normalizeAddressList } from 'ken-all/esm/address';

test('hoge', () => {
  const sum = ''
  expect(sum).toEqual(sum)
})
